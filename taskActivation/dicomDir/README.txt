Scanner DICOMs go here (live mode, when dsAccessionNumber = 'None' in the toml).
Name them to match dicomNamePattern. For debugging without a scanner, leave this
empty and keep dsAccessionNumber = 'ds000244' to replay the real HcpMotor run.
